# Notebooks

Разведочный анализ сезонности: даты заказов, категории товаров, объёмы продаж, выручка, средний чек, freight, повторяемость по месяцам и периодам.

Повторяемую логику лучше переносить в `src/` или `scripts/`.
